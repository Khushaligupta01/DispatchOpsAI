# ==================================================
# DispatchOps AI — Python Dependencies
# Target: Python 3.10
#
# All versions are pinned for reproducibility.
# Every package here has a pre-built wheel for Python 3.10
# — no source compilation required on install.
# ==================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0          # ASGI server with hot reload support

# --- Configuration ---
pydantic==2.7.1
pydantic-settings==2.2.1           # Loads .env into Pydantic Settings models

# --- Database ---
sqlalchemy==2.0.30                 # Async ORM (full py3.10 support)
asyncpg==0.29.0                    # Async PostgreSQL driver
alembic==1.13.1                    # Database schema migrations

# --- Task Queue ---
celery==5.4.0
redis==5.0.4                       # Redis client — broker + result backend

# --- AI / LLM ---
openai-whisper==20231117           # Local speech-to-text (requires ffmpeg)
langchain==0.2.1                   # LLM orchestration framework
langchain-groq==0.1.4              # Groq provider for LangChain

# --- Observability ---
langfuse==2.36.1                   # AI pipeline tracing

# --- Telephony ---
twilio==9.0.4                      # Twilio voice webhooks + recording download

# --- Data Processing ---
pandas==2.2.2
numpy==1.26.4

# --- HTTP Client ---
httpx==0.27.0                      # Async HTTP client (also FastAPI test client)

# --- Logging ---
python-json-logger==2.0.7          # Structured JSON log formatter

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6             # Async test support
pytest-mock==3.14.0                # Mocking utilities

# --- Dev Tooling ---
python-dotenv==1.0.1               # Load .env file in local development
